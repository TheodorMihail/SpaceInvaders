namespace SpaceInvaders.Project
{
    /// <summary>
    /// Enum representing all scenes in the Space Invaders project.
    /// Scene names must match exactly with Unity Build Settings.
    /// </summary>
    public enum SceneTypes
    {
        Preload,
        MainMenu,
        Campaign,
        Game
    }
}
